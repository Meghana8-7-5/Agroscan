{
  "name": "agroscan-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd .. && pnpm install --no-frozen-lockfile && pnpm run vercel-build",
    "vercel-build": "cd .. && pnpm install --no-frozen-lockfile && pnpm run vercel-build"
  }
}
